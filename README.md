# base64encoder
a short c++ program that encodes a file into its base64 representation

compile the source: `g++ -std=c++11 -Wall encode.cpp`

and run with filepath as cli argument: `./a.out <filepath>`

any comments are welcome. thx
